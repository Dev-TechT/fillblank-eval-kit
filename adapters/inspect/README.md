# Example Inspect AI adapter

This is a lightweight starter. Install the optional dependency before use:

```bash
uv run --with inspect-ai python adapters/inspect/fillblank_task.py
```

The canonical schema and validator live in `src/fillblank_eval/`.
